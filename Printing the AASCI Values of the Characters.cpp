//Printing the ASCI values
#include<iostream>
using namespace std;

int main()
{
	char ch='a';
	cout<<"The ASCI value of the character is "<<int(ch)<<endl;
	/*
	String is actually a class and all the string variables are it's objects.
	
	Char Array is user defined data type.
	
	In char array there is static memory allocation where as in string there is
	dynamic memory allocation so no memory is wasted.
	*/
	return 0;
}

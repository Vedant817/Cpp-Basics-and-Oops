#include<iostream>
using namespace std;

int main()
{
	/*
		Type Conversions are automatic if the  quantity is a built-in function otherwise the conversion is not automatic.
		They are of three types: class to basic, basic to class and class to class.Here type souce is the basic type.
		These are performed by two ways:Constructor r Operator Overloading.
	*/
	
	return 0;
}

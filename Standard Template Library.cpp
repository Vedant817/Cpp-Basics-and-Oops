#include<iostream>
using namespace std;

int main()
{
//	Templates are actually used for providing the reusability of the cde.
//	We can form number of classes from the same template and that too of  different data-types.
//	This is sometimes also called parameterised classes.
	/*
		template<class T>
		class vector{T*arr...}
		main(){
			vector<int>myvcc(ptr);
			vector<float>myfvcc(ptr);
		}
	*/
	
	return 0;
}


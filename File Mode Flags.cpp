#include<iostream>
#include<fstream>
#include<string>
using namespace std;

int main()
{
	/*
		1. ios::app->Will append the input data without affecting the existing data.
		2. ios::ate->The input may be wriiten anywhere in the file.
		3. ios::binary->File is opened in the binary format and can be read/write in the pure binay format.
		4. ios::noreplace->If the file exist it will not allow the file to open.
		5. ios::trunc->If the file exist,it will delete all the content inside it.
	*/
	
	return 0;
}
